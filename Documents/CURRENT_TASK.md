# 当前任务

- 任务：修复托盘区域松手后碎片未回收
- 状态：代码已完成，等待 Play Mode 验收
- 更新时间：2026-08-22

## 用户意图

- 从托盘拿起 Piece 后，只要松手位置或 Piece 本身进入底部暗色托盘区域，就必须触发判定并自动回到托盘位置。
- 修复 `CardBag022` 高频出现的漏判，同时保持棋盘左右桌面可自由放置的既有规则。
- 保留第一版托盘屏幕尺寸适配：Piece 拿起后只能保持尺寸或放大，不允许缩小。

## 工作记录

- 确认漏判后会被“Piece 完整位于棋盘左侧或右侧即可自由放置”的规则接住，因此停在托盘上方或暗色区附近。
- 可见托盘下，松手判定新增 Piece 与托盘稳定世界边界的直接重叠检查。
- 托盘原始回收热区改用根 Canvas 设计坐标到游戏相机的稳定映射进行缓存，不再依赖相机适配后可能尚未刷新的 UI 世界角点。
- 托盘隐藏后仍继续使用缓存的原始区域作为回收热区。
- 未修改托盘 Piece 缩放、拿起尺寸、正确吸附、错误回弹以及棋盘左右桌面自由放置规则。

## 修改文件

- `Assets/Scripts/Controller/GameScene.cs`
- `Documents/CURRENT_TASK.md`

## 决策

- 稳定坐标只用于托盘回收热区缓存；托盘尺寸计算继续保留第一版实际屏幕矩形适配，避免改变已经确认正确的缩放效果。
- 可见托盘优先使用布局本身采用的稳定世界边界做重叠判定，缓存屏幕矩形继续处理指针命中以及托盘隐藏后的回收。

## 验证

- `dotnet build Assembly-CSharp.csproj --no-restore`：通过，`0` 警告，`0` 错误。
- `dotnet build Assembly-CSharp-Editor.csproj --no-restore`：通过，`0` 警告，`0` 错误。
- 尚未在 Unity Play Mode 中执行鼠标拖放验收。

## 下一步

1. 在 `CardBag022` 从托盘拿起 Piece，分别测试指针进入托盘、指针在外但 Piece 边缘进入托盘，确认均自动回收。
2. 收起托盘后把桌面 Piece 拖入托盘原始区域，确认托盘自动显示并回收 Piece。
3. 在棋盘左右两侧且完整高于托盘的位置松手，确认仍允许放在桌面上。

## 恢复提示

继续 Puffies 的“托盘区域松手回收”任务。先阅读 `AGENTS.md`、`Documents/WORKFLOW.md` 和 `Documents/CURRENT_TASK.md`；保留第一版托盘屏幕尺寸适配，不得恢复第二版世界边界强制放大方案。优先在 `CardBag022` 验证托盘可见和隐藏时的回收热区，并确认棋盘左右桌面自由放置未受影响。未经用户明确要求不要自动提交；用户要求提交时同时推送。
