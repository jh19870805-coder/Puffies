# 当前任务

- 任务：正确 Piece 顶回错误占位块并统一回托盘反馈
- 状态：代码、规则和编译验证完成，等待 Unity Play Mode 视觉确认
- 更新时间：2026-08-11

## 用户意图

- 错误 Piece 松手后不要立即变红，应先开始回弹。
- Piece 进入黑色托盘区域后才变红。
- 红色提示比现有效果淡 `30%`。
- 正确 Piece 放到被错误 Piece 占用的槽位时，错误 Piece 顶回托盘，正确 Piece 正常吸附。
- Piece 只要从桌面或棋盘外部回到托盘，都执行进入托盘后的红色错误提示。

## 工作记录

- 删除错误回弹开始前的红色赋值和 `0.08s` 停顿，保持现有 `0.3s` 三次方减速回弹。
- 回弹期间逐帧检测 Piece 渲染边界与可见黑色托盘边界，首次相交时才显示红色。
- 红色由 Piece 原色向现有错误红混合 `70%`，Alpha 保持原值，避免通过半透明削弱 Piece 本身。
- 正确吸附前收集目标凹槽实际轮廓覆盖的全部外部 Piece，将这些错误块加入托盘布局并分别回弹；正确 Piece 同时继续正常吸附。
- 外部 Piece 手动进入托盘时同样改走错误回弹动画，不再直接瞬间缩放和普通重排。
- Piece 回弹与正确吸附可并行，交互锁改为计数方式，全部动画结束前不会提前恢复拖拽。
- 外部位置之间的原位回弹仍不显示托盘红色反馈。

## 修改文件

- `Assets/Scripts/Controller/GameScene.cs`
- `Documents/PROJECT_CONTEXT.md`
- `Documents/GAME_DESIGN_REQUIREMENTS.md`
- `Documents/CURRENT_TASK.md`
- `specs/spec-driven-development.md`

## 决策

- “进入黑色托盘区域”按 Piece 和托盘的二维世界渲染边界首次相交计算，不等待进入面积达到 `50%`。
- “红色变淡 30%”按现有错误红着色强度保留 `70%` 处理，不降低 Piece Alpha。
- 正确吸附优先于错误占位：占位块不能再让正确 Piece 失败。
- 被顶回的全部占位块按现有 Piece 编号和固定间距重新进入托盘，不保留其外部位置。
- 不修改吸附距离、轮廓重叠标准、回弹时长、托盘间距或持久化结构。
- 本次不修改场景、Prefab、资源或持久化结构，不需要清理本地数据。

## 验证

- 静态确认松手后不再立即赋红，也不再执行 `InvalidDropHoldDuration` 停顿。
- 静态确认所有目标为托盘的回弹都会先设置 `state.IsOnTray`，并在 Piece 首次进入可见托盘边界时设置错误红色。
- 静态确认正确 Piece 的目标槽位存在外部占位块时，正确 Piece 继续吸附，占位块改为回托盘。
- 静态确认手动把外部 Piece 放回托盘时也调用同一个错误回弹动画。
- `Assembly-CSharp.csproj` 与 `Assembly-CSharp-Editor.csproj` 编译通过，均为 `0` 警告、`0` 错误。
- 尚未在 Unity Play Mode 目视确认回弹时序和红色强度。

## 下一步

1. 将错误 Piece 放在另一块的正确槽位，再放入正确 Piece，确认正确 Piece 吸附且错误 Piece 回托盘并闪红。
2. 手动把桌面 Piece 拖回仍有内容的托盘，确认进入托盘后同样闪红并按编号重排。
3. 从桌面错误位置触发原位回弹，确认不经过托盘时不显示红色。

## 恢复提示

正确 Piece 已能把槽位上的外部错误 Piece 顶回托盘；外部 Piece 手动回托盘也统一使用进入托盘后的 `70%` 强度红色反馈。两个 C# 项目已编译通过，下一步进入 Unity Play Mode 验证三类路径。
