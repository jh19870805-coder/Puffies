# 当前任务

- 任务：修正新手引导显示与贴图吸附反馈
- 状态：代码实现与编译验证完成，等待 Play Mode 视觉确认
- 更新时间：2026-07-31

## 用户意图

- `CardBag001` 新手引导从第二步开始显示当前组关卡描边。
- 正确放置贴图时不能直接瞬移到凹槽；需要先播放吸附缓动，到位后再显示现有闪光。
- 新手引导提示文字必须完全使用 `GameScene/GuideTip/TextTips` 在编辑器中设置的样式，运行时代码不得覆盖。

## 工作记录

- 将“教程焦点展示”和“教程阻止关卡描边”拆成两个状态：第1步继续隐藏关卡描边，第2步保留双贴纸高亮并恢复当前组烘焙描边，第3步行为不变。
- 正确放置后，运行时贴图从松手位置以三次方减速曲线在 `0.18s` 内吸附到凹槽中心。
- 吸附抵达后才切换为棋盘内的 Piece Image、播放原有 `0.3s` 绿色闪光并判断切组或结算。
- 吸附期间锁定新的贴图拖拽、提示按钮和“一键完成”，并让鼠标恢复普通状态，防止并发操作破坏分组状态。
- 删除运行时创建的 29 号白字、自动字号和额外阴影；三步提示直接克隆场景 `GuideTip/TextTips`，只替换文案并关闭射线检测。
- 拼图进度仍在判定正确后立即保存；本次未修改 SQLite 或 JSON 数据结构，无需删除本地数据。

## 修改文件

- `Assets/Scripts/Controller/GameScene.cs`
- `Documents/CURRENT_TASK.md`
- `Documents/PROJECT_CONTEXT.md`

## 决策

- 第二步恢复的是当前组烘焙关卡描边，不增加提示按钮的绿色虚线；提示按钮仍到第三步才出现。
- 吸附动画只改变正确放置的视觉时序，不改变吸附距离、贴图缩放比、进度保存、切组或结算规则。
- 使用减速吸附，不增加回弹或过冲，避免不规则贴图在凹槽边缘产生短暂错位。
- 编辑器中的 `TextTips` 是提示文字样式唯一来源；代码不再写入字体、字号、材质、颜色、粗体、对齐、边距或阴影参数。

## 验证

- `dotnet build Puffies.sln --no-restore`：通过，0 警告、0 错误。
- `git diff --check`：通过，仅有仓库现有的 LF/CRLF 转换提示。
- 已检查第二步教程焦点重建仍正常，且不会再次清除关卡描边。
- 已确认代码只克隆 `GuideTip/TextTips` 并更新 `text`，C# 编译通过。
- 尚未在 Unity Play Mode 中确认实际吸附节奏和第二步描边视觉。

## 下一步

1. 重玩 `CardBag001`，完成第1组后确认第二步立即显示当前组烘焙描边。
2. 在不同距离内松开贴图，确认贴图以相同 `0.18s` 节奏减速吸附，到位后再闪光。
3. 检查第二步第一片贴图吸附后，剩余贴图高亮和提示文字正常，描边不消失。
4. 检查每组最后一片吸附完成后才开始棋盘切组或结算，不发生提前切换。
5. 检查三步提示文字与编辑器 `GuideTip/TextTips` 的字体、字号、颜色、材质、粗体和布局完全一致。

## 恢复提示

继续 Puffies 当前任务。第二步描边、`0.18s` 减速吸附和编辑器 `GuideTip/TextTips` 样式复用已实现；下一步在 Unity Play Mode 回归提示样式、吸附闪光和切组时序。
