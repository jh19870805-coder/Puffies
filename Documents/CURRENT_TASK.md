# 当前任务

- 任务：校正拆包滑光与星星的尺寸比例
- 状态：整体横向拉伸代码和编译验证完成，等待 Play Mode 视觉确认
- 更新时间：2026-08-13

## 用户意图

- 对照制作方初始特效，保持星星和光带合理的尺寸比例。
- 只调整光带缩放，不修改星星、卡包、Stage、位置、材质或播放时序。

## 工作记录

- 核对 `fx_chai_w_001.prefab`：`line`、`line01`、`glow`、`glowC` 与 `dot`、`dot01`、`glow01` 的原始 Transform Scale 均为 `1`。
- 当前星星节点保持原始 Scale，但四个光带节点使用 `7.0x × 1.4x`，纵向额外放大破坏了制作方原始光带厚度与星星高度比例。
- 按“只延长光带、厚度保持初始值”的规则，将四个光带节点调整为 `7.0x × 1.0x`。
- 继续核对发现四个光层的原始中心位置并不一致：主 `line` 位于 `localX=-3.3`，其余光层接近根中心。此前只缩放每层自身宽度，没有同步拉开各层相对根中心的位置，导致左侧渐隐光尾没有随组合整体延长。
- 将四个光层的 `localPosition.x` 与自身 X Scale 同时乘以 `7.0`，以光效根中心为共同横向拉伸锚点；Y/Z、厚度和星星节点不变。

## 修改文件

- `Assets/Scripts/Controller/MainScene.cs`
- `Documents/CURRENT_TASK.md`

## 决策

- 横向长度继续使用已确认的 `7.0x`。
- 纵向厚度恢复 Prefab 初始 `1.0x`，以未缩放星星作为比例基准。
- “整体拉伸”定义为光层自身宽度和相对根中心的 X 偏移使用同一长度倍率，不能只围绕各层自身中心放大。
- 不调整三个星形节点，也不调整光效根节点和其他开包表现。

## 验证

- 静态确认仅 `LightBandHeightScale` 从 `1.4f` 改为 `1f`。
- `Assembly-CSharp.csproj` 与 `Assembly-CSharp-Editor.csproj` 编译通过，均为 `0` 警告、`0` 错误。
- `git diff --check` 通过；代码差异确认只有 `LightBandHeightScale` 从 `1.4f` 改为 `1f`。
- 整体横向拉伸调整后再次编译两个程序集，均为 `0` 警告、`0` 错误；运行时代码只遍历四个光层，并同步缩放其 `localPosition.x` 与 X Scale，未引用或修改三个星星节点。
- 待在 Play Mode 对比星星高度与光带厚度。
- 待确认左侧光尾与中央亮芯按原始组合关系共同延长，不再出现尾部偏短。

## 下一步

1. 进入 MainScene 播放拆包动画，确认光带仍保持当前长度，但厚度恢复接近制作方初始画面。
2. 确认星星尺寸、分布和位置完全没有变化。

## 恢复提示

当前仅将四个拆包光带节点从 `7.0x × 1.4x` 调整为 `7.0x × 1.0x`。下一步进入 MainScene Play Mode 对比星星和光带比例。
