# 当前任务

- 任务：结算页面轮播实际加分
- 状态：代码与编译验证已完成，等待 Play Mode 视觉确认
- 更新时间：2026-08-11

## 用户意图

- 结算页面原“卡包数”标题位置先依次显示本局生效的加分项。
- 标题初始为空，第一次显示的内容必须是首个加分项。
- 加分项显示实际增加的分数，不显示百分比。
- 全部加成展示完成后，标题恢复“卡包数”。
- 所有标题内容限制为一行，宽度不足时自动缩小字号。

## 工作记录

- 恢复四类加成在 `TaskScoreTitle` 上的阶段展示，格式为“加成名称 +N分”。
- 每项实际加分取当前阶段目标分减去上一阶段分，累计结果与最终分严格一致。
- RewardPanel 初始化和基础分滚动期间标题保持空白；全部加成阶段结束后显示“卡包数”。
- 标题关闭自动换行并启用 TMP Auto Size，编辑器当前字号作为最大字号。

## 修改文件

- `Assets/Scripts/Controller/GameScene.cs`
- `Documents/GAME_DESIGN_REQUIREMENTS.md`
- `Documents/PROJECT_CONTEXT.md`
- `Documents/CURRENT_TASK.md`

## 决策

- 不单独对每个百分比取整；实际加分按现有累计阶段取整结果的差值展示，避免展示项之和与最终分不一致。
- 不修改字体、颜色、字重、对齐或文本容器，只增加单行和自动缩小约束。
- 不修改积分比例、任务累计、完成卡包数统计、奖励发放或持久化结构，不需要清理本地数据。

## 验证

- `Assembly-CSharp.csproj` 与 `Assembly-CSharp-Editor.csproj` 顺序编译通过，均为 `0` 警告、`0` 错误。
- 场景 `TaskTitle2` 原始字号为 `36`、Auto Size 下限为 `18`、文本框宽度为 `500`；运行时保持 `36` 为字号上限并允许缩小至 `18`。
- 待在 Play Mode 验证四种加成组合、长文案单行缩放以及最终“卡包数”恢复。
- 待确认标题初始与基础分滚动期间保持空白，第一条可见文字为加分项。

## 下一步

1. 在 Play Mode 完成一包，确认加分项、实际分值、单行缩放和最终标题。
2. 退出 Play Mode 后执行 **Puffies -> Bake Outline Masks**，继续验证上一项多关卡描边通用修复。

## 恢复提示

结算标题初始为空，第一条可见文字为实际加分项，完成后显示“卡包数”；标题强制单行并自动缩小。两个 C# 项目已重新编译通过，下一步 Play Mode 视觉验证。
